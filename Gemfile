source 'https://rubygems.org'

# chef is the main tool we're using to provision a server
gem 'chef'

# knife solo simplifies chef provisioning because chef server is not needed
gem 'knife-solo'

# Chef has cookbook, that are like gems in ruby.
# Berkshelf enables easy cookbook installation. It is a bundler equivalent for chef.
gem 'berkshelf'
